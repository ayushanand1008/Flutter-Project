name: drive_preflight_tool
description: Standalone CLI tool to test drive.file scope subfolder inheritance.
version: 1.0.0
publish_to: none

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  googleapis: ^12.0.0
  googleapis_auth: ^1.6.0
  http: ^1.2.0
